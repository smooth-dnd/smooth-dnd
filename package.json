{
  "name": "smooth-dnd",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "lerna": "lerna",
    "prettier": "prettier packages/**/*.{ts,tsx,js,json,vue} --write"
  },
  "dependencies": {
    "lerna": "^6.6.1",
    "prettier": "^2.8.8",
    "typescript": "^5.0.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
